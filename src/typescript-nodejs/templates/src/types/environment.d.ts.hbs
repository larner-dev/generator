{{#includesAny features "log_management" "koa_api"}}
declare global {
  namespace NodeJS {
    interface ProcessEnv {
      NODE_ENV: "development" | "production";
      PORT: string;
      {{#includes features "log_management"}}
      LOG_LEVEL:
        | "trace"
        | "debug"
        | "info"
        | "warn"
        | "error"
        | "fatal"
        | "silent";
      {{/includes}}
    }
  }
}

export {};
{{/includesAny}}

